/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "https://www.pexels.com/",
      "cdn.discordapp.com",
      "images.pexels.com",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
